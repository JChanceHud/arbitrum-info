{
  "name": "arbitrum-info",
  "version": "1.0.0",
  "description": "This repo contains everything needed to deploy an Arbitrum rollup chain.",
  "main": "index.js",
  "scripts": {
    "deploy": "sh scripts/deploy.sh",
    "deploy:rollup": "sh scripts/deploy_rollup.sh",
    "deploy:contract": "npm run compile && node scripts/deploy_contract.js",
    "compile": "sh scripts/compile.sh",
    "start:validator": "sh scripts/start.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chance Hudson",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "web3": "^1.3.0"
  }
}
